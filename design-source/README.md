# Sparkle Auto design source

This directory is the non-production source library introduced by the v3.1 contract.
Only optimized assets whose manifest status is `approved` may move into `public/assets`.

The numbered directories mirror Boards B01–B12 and the production pipeline. They are
populated and approved during GROUP 01; GROUP 00 establishes the boundary only.
