# Server-only values. Never prefix these with NEXT_PUBLIC_ and never commit real secrets.
SUPABASE_URL=https://iomwmbtnvhpegngouonw.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
# Generate at least 32 random bytes, for example: openssl rand -base64 32
SPARKLE_RATE_LIMIT_SECRET=

# Public canonical origin used by metadata once the production domain is approved.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
